# Third-Party Dependencies
# ========================
#
# Submodules:
#   ice_zero_prog/  — blackmesalabs/ice_zero_prog (Python FPGA programmer)
#
# Note: yosys, nextpnr, and icestorm are built inside Docker only.
#       See Dockerfile for build details.
#
# Init:  git submodule update --init --recursive

.PHONY: all clean

all:
	@echo "No native third-party builds required."
	@echo "FPGA toolchain (yosys/nextpnr/icestorm) is built inside Docker."

clean:
